---
import { getCollection, render } from "astro:content";
import Header from "../../components/projects/Header.astro";
import Links from "../../components/projects/Links.astro";
import Cover from "../../components/projects/Cover.astro";
import TechStack from "../../components/projects/TechStack.astro";
import Layout from "../../layouts/Layout.astro";

export async function getStaticPaths() {
  const projects = await getCollection("projects");

  return projects.map((project) => ({
    params: { slug: project.data.slug },
    props: { project },
  }));
}
const { project } = Astro.props;
const { Content } = await render(project);
const { title, url, github, cover, mainTech } = project.data;
---

<Layout title={title}>
  <main class="flex flex-col h-full w-full max-w-2xl gap-6">
    <Header title={title} />

    <div>
      <Links url={url} github={github} />
    </div>

    <div>
      <Cover cover={cover} title={title} />
    </div>

    <div>
      <TechStack mainTech={mainTech} />
    </div>

    <section class="flex flex-col gap-6 content-list max-w-none">
      <Content />
    </section>
  </main>
</Layout>
