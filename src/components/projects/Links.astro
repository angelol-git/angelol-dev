---
import { Globe } from "@lucide/astro";
import Github from "../../components/icons/Github.astro";
const { url, github } = Astro.props;
---

<div class="flex gap-6 items-center">
  {
    url && (
      <a
        href={url}
        target="_blank"
        rel="noopener noreferrer"
        class="flex gap-2 items-center text-accent"
      >
        <Globe size={18} class="stroke-1" />
        <div class="font-mono text-sm">Live Site</div>
      </a>
    )
  }

  {
    github && (
      <a
        href={github}
        target="_blank"
        rel="noopener noreferrer"
        class="flex gap-2 items-center group text-ink-muted hover:text-accent transition-colors duration-150"
      >
        <Github />
        <div class="font-mono text-sm">Source Code</div>
      </a>
    )
  }
</div>
